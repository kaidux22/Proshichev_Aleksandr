doctype html
html
    head
        meta(charset="utf-8")
        title Home library
    body
        header
            .header_name Библиотека
        .user
            .user_name.
                #[b Имя пользователя:] #{user_name}
        section
            u1
                each book in books 
                    li 
                        a
                            =`${book.id}: ${book.title} (${book.author} ${book.dataRelease})`